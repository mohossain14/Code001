#include <stdio.h>

int main(void){
    int day, start, i;

    printf("Enter number of days in month: ");
    scanf("%d", &day);
    printf("Enter starting day of the week: ");
    scanf("%d", &start);

    for(i = 1, i <= day; i++){
        
    }

}