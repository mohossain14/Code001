//gives the closest flight time to given time range

#include <stdio.h>

int main(void){
    int hr, min, totalTime;

    printf("Enter a 24-hour time: ");
    scanf("%d:%d", &hr, &min);

    totalTime = (hr * 60) + min;

    
}